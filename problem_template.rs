#![allow(non_snake_case)]

use proconio::{input, fastout};

#[fastout]
fn main() {
    input! {
        N: usize,
        A: [u64; N]
    }

    let mut answer: u64 = 0;

    println!("{}", answer);
}
